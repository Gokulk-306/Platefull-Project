apiVersion: v1
kind: Service
metadata:
  name: plateful-service
  labels:
    app: plateful
spec:
  type: NodePort
  selector:
    app: plateful
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
